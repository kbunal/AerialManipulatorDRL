from setuptools import setup

setup(name='AerialManipulatorDRL',
      version='0.0.1',
      install_requires=['gym', 'tensorflow', 'stable-baselines', 'numpy']  # And any other dependencies foo needs
      )
